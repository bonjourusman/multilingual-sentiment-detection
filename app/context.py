import os
import sys
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

from train import helpers

# Used to import 'helpers' module from 'train' package stored in the root dir of this repo.